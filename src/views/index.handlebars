<div class="container">

  <div class="row">
    <div class="col-md-4">
      {{> tasks/taskForm}}
    </div>
    <div class="col-md-8">
      {{> tasks/taskTable}}
    </div>
  </div>

{{#if error}}
<p class="alert alert-info">{{error}}</p>

{{/if}}
</div>